let db,project,selectedWeek=Number(new URLSearchParams(location.search).get('s'))||1;init();async function init(){nav('semana');db=await api('getDB');project=activeProject(db);selectedWeek=Math.min(selectedWeek,project.weeks);renderHeader(db,project,'semana.html');renderWeek()}function renderWeek(){document.getElementById('weekBtns').innerHTML=Array.from({length:project.weeks},(_,i)=>`<button class="${selectedWeek===i+1?'active':''}" onclick="selectedWeek=${i+1};renderWeek()">S${i+1}</button>`).join('');let planned=(project.items||[]).filter(x=>x.plan===selectedWeek||x.reprogram===selectedWeek);let html=`<h3>Semana ${selectedWeek}</h3><p class="muted">Plan de esta semana</p>`;planned.forEach(it=>html+=`<label class="check"><input type="checkbox" ${it.actual===selectedWeek?'checked':''} onchange="toggleActual('${it.id}',this.checked)"><span>${it.icon||''} ${it.name}</span></label>`);html+=`<div class="formrow"><input id="extra" placeholder="Acción nueva realizada"><button class="btn" onclick="addExtra()">Agregar</button></div><div class="formrow"><input id="budgetInput" type="number" placeholder="Presupuesto real S${selectedWeek}" value="${project.actualBudget?.[selectedWeek-1]??''}"><button class="btn primary" onclick="saveWeekly()">Guardar</button></div><div id="msg"></div>`;document.getElementById('weeklyForm').innerHTML=html;let pend=planned.filter(x=>x.actual!==selectedWeek);document.getElementById('pendingBox').innerHTML=pend.length?pend.map(it=>`<div class="listrow"><b>${it.name}</b><span>Mover a</span><select onchange="moveItem('${it.id}',this.value)">${Array.from({length:project.weeks},(_,i)=>`<option value="${i+1}" ${it.reprogram===i+1?'selected':''}>S${i+1}</option>`).join('')}</select></div>`).join(''):'<p class="muted">Sin pendientes esta semana.</p>'}function item(id){return project.items.find(x=>x.id===id)}function toggleActual(id,checked){let it=item(id);it.actual=checked?selectedWeek:null;if(!checked&&!it.reprogram)it.reprogram=Math.min(selectedWeek+1,project.weeks);saveWeekly()}function moveItem(id,w){item(id).reprogram=Number(w);saveWeekly()}function addExtra(){let v=document.getElementById('extra').value.trim();if(!v)return;project.items.push({id:'a'+Date.now(),icon:'➕',name:v,plan:selectedWeek,actual:selectedWeek});saveWeekly()}async function saveWeekly(){let val=document.getElementById('budgetInput')?.value;if(val!==undefined)project.actualBudget[selectedWeek-1]=val===''?null:Number(val);await api('saveWeekly',{projectId:project.id,items:project.items,actualBudget:project.actualBudget});let local=localDB();local.projects[project.id]=project;saveLocal(local);document.getElementById('msg')?.classList.add('notice');if(document.getElementById('msg'))document.getElementById('msg').textContent='Guardado.';renderWeek()}
