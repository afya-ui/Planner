requireAuth();let {p}=renderProjectShell('resumen');let m=projectMetrics(p);let pending=(p.items||[]).filter(x=>x.actual==null).slice(0,8);document.getElementById('summary').innerHTML=`<section class="stats"><div class="stat"><span class="muted">Avance</span><strong>${m.progress}%</strong></div><div class="stat"><span class="muted">Acciones</span><strong>${m.done}/${m.total}</strong></div><div class="stat"><span class="muted">Presupuesto real</span><strong>${money(m.real)}</strong></div><div class="stat"><span class="muted">Semana actual</span><strong>${m.current}</strong></div></section><section class="panel"><div class="box"><h2>Presupuesto</h2><p class="muted">Plan: ${money(m.plan)}</p><p class="muted">Real: ${money(m.real)}</p><div class="notice">${m.budgetStatus}</div></div><div class="box"><h2>Próximos pendientes</h2>${pending.length?pending.map(x=>`<div class="listrow"><span>${x.icon||'•'} ${x.name}</span><b>S${x.reprogram||x.plan}</b><span></span></div>`).join(''):'<p class="muted">No hay pendientes.</p>'}</div></section>`;
